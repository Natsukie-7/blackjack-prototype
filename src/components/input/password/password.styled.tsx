import { styled } from "solid-styled-components";
import DefaultStyled from "../input.styled";

const Wrapper = styled(DefaultStyled.Wrapper)``;

const Label = styled(DefaultStyled.Label)``;

const Text = styled(DefaultStyled.Input)``;

const InvalidText = styled(DefaultStyled.InvalidText)``;

const Styled = {
  Wrapper,
  Label,
  Text,
  InvalidText,
};

export default Styled;
